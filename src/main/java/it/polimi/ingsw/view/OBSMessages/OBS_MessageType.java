package it.polimi.ingsw.view.OBSMessages;
/**Type of message sends by client's input stream (or scanner)*/
public enum OBS_MessageType {
    NUMBER_OF_PLAYERS, CHANGED_USERNAME, MOVE, INITIAL_INFO, CHAT, START;
}
